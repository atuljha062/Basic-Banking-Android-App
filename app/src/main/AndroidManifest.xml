<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atul.banking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dashboard"
            android:label="Android Bank"></activity>
        <activity android:name=".TransactionHistory"
            android:label="Transactions"/>
        <activity android:name=".SelectFromAllUser"
            android:label="Select User"/>
        <activity android:name=".TransferMoney"
            android:label="Transfer Money"/>
        <activity android:name=".IndividualUser"
            android:label="User Details"/>
        <activity android:name=".AllUsers"
            android:label="Users"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>